/.idea/
/.output.txt
node_modules/

# Playwright
/e2e-tests/playwright-report/
/e2e-tests/test-results
/e2e-tests/test-results.log
# The e2e job runs the Playwright image with HOME=/work (see ci.yml), so npm
# drops its cache and logs into the checkout rather than into a real home dir.
/e2e-tests/.cache/
/e2e-tests/.npm/

# Go coverage
/wsgrpc/coverage
/wsgrpc/coverage.out

# `go build` output in example/server (the dir and the binary share a name)
/example/server/server
/example/server/server.exe
